<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results - <%= student.name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #0a1e46;">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-graduation-cap me-2"></i>
                OMR Result Viewer
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">
                            <i class="fas fa-home me-1"></i>Home
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container my-4">
        <div class="row">
            <div class="col-12">
                <!-- Student Info Header -->
                <div class="card mb-4">
                    <div class="card-header text-white" style="background-color: #34495e;">
                        <h4 class="mb-0">
                            <i class="fas fa-user-check me-2"></i>
                            Welcome, <%= student.name %>
                        </h4>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong>Roll Number:</strong> <%= student.rollNo %></p>
                                <p><strong>Post Applied:</strong> <%= student.postApplied %></p>
                            </div>
                            <div class="col-md-6">
                                <p><strong>Date of Birth:</strong> <%= new Date(student.dob).toLocaleDateString('en-GB') %></p>
                                <p><strong>Mobile:</strong> <%= student.mobile %></p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Priority Display Logic: Results > OMR > No Data Message -->
        <% if (hasResults && isResultsPublic) { %>
            <!-- Show Results (Highest Priority) -->
            <div class="row">
                <div class="col-12 mb-4">
                    <div class="card">
                        <div class="card-header text-white" style="background-color: #34495e;">
                            <h5 class="mb-0">
                                <i class="fas fa-trophy me-2"></i>
                                Your Exam Results
                            </h5>
                        </div>
                        <div class="card-body">
                            <!-- Final Score Display -->
                            <div class="text-center mb-4">
                                <div class="display-4 fw-bold text-primary">
                                    <%= student.results.finalScore %>/100
                                </div>
                                <div class="h5 text-muted">Final Score</div>
                                <div class="h6">
                                    <span class="badge bg-secondary fs-6"><%= student.results.percentage %>%</span>
                                </div>
                            </div>

                            <!-- Score Breakdown -->
                            <div class="row text-center mb-4">
                                <div class="col-md-4 mb-3">
                                    <div class="card border-success">
                                        <div class="card-body">
                                            <div class="display-6 text-success fw-bold">
                                                <%= student.results.correctAnswers %>
                                            </div>
                                            <div class="text-success fw-semibold">Correct Answers</div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <div class="card border-danger">
                                        <div class="card-body">
                                            <div class="display-6 text-danger fw-bold">
                                                <%= student.results.wrongAnswers %>
                                            </div>
                                            <div class="text-danger fw-semibold">Wrong Answers</div>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-4 mb-3">
                                    <div class="card border-warning">
                                        <div class="card-body">
                                            <div class="display-6 text-warning fw-bold">
                                                <%= student.results.unattempted %>
                                            </div>
                                            <div class="text-warning fw-semibold">Unattempted</div>
                                        </div>
                                    </div>
                                </div>
                            </div>


                        </div>
                    </div>
                </div>
            </div>
        <% } else if (hasOMR && isOmrPublic) { %>
            <!-- Show OMR Sheet (Second Priority) -->
            <div class="row">
                <div class="col-12 mb-4">
                    <div class="card">
                        <div class="card-header text-white" style="background-color: #34495e;">
                            <h5 class="mb-0">
                                <i class="fas fa-file-image me-2"></i>
                                Your OMR Sheet
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="alert alert-light mb-3">
                                <i class="fas fa-info-circle me-2"></i>
                                Your OMR sheet is available for review. Results will be published soon!
                            </div>
                            <div class="text-center">
                                <% 
                                    const secureOmrUrl = `/secure/omr/${student.rollNo}`;
                                %>
                                <% if (student.omrImageUrl.includes('.pdf')) { %>
                                    <iframe src="<%= secureOmrUrl %>" 
                                            width="100%" height="600" 
                                            style="border: 1px solid #ddd;">
                                        <p>Your browser does not support PDFs. 
                                           <a href="<%= secureOmrUrl %>" target="_blank">Download the PDF</a>
                                        </p>
                                    </iframe>
                                <% } else { %>
                                    <img src="<%= secureOmrUrl %>" 
                                         alt="OMR Sheet for <%= student.rollNo %>" 
                                         class="img-fluid border rounded"
                                         style="max-height: 600px;">
                                <% } %>
                                <div class="mt-3">
                                    <a href="<%= secureOmrUrl %>" target="_blank" class="btn text-white" style="background-color: #34495e;">
                                        <i class="fas fa-external-link-alt me-1"></i>
                                        View Full Size
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% } else { %>
            <!-- Data Not Published Yet -->
            <div class="row">
                <div class="col-12 mb-4">
                    <div class="card">
                        <div class="card-body text-center py-5">
                            <i class="fas fa-cogs fa-3x text-muted mb-3"></i>
                            <h4 class="text-muted">Data Being Processed</h4>
                            <p class="text-muted">
                                Your data is being processed and will be published soon. Please check back later.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- Action Buttons -->
        <div class="row mt-4">
            <div class="col-12 text-center">
                <a href="/logout" class="btn me-2" style="border: 1px solid #34495e; color: #34495e;">
                    <i class="fas fa-sign-out-alt me-1"></i>
                    Logout
                </a>
                <a href="/answer-key?post=<%= student.postApplied %>" class="btn me-2" style="border: 1px solid #28a745; color: #28a745;">
                    <i class="fas fa-key me-1"></i>
                    View Answer Key (<%= student.postApplied %>)
                </a>
                <button onclick="window.print()" class="btn" style="border: 1px solid #6c757d; color: #6c757d;">
                    <i class="fas fa-print me-1"></i>
                    Print Results
                </button>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-light text-center py-3 mt-5">
        <div class="container">
            <p class="mb-0 text-muted">
                &copy; <%= new Date().getFullYear() %> OMR Result Viewer. All rights reserved.
            </p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    
    <style>
    @media print {
        .btn, .navbar, footer {
            display: none !important;
        }
        .card {
            border: 1px solid #000 !important;
            box-shadow: none !important;
        }
    }
    </style>
</body>
</html>